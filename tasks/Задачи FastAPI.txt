🟢 1. Простое REST API: Список задач

Уровень: новичок
Цель: понять основы FastAPI: маршруты, модели, простая структура.

Описание задачи:
Создай API, которое позволяет:

Получать список всех задач

Получать одну задачу по ID

Добавлять новую задачу

Удалять задачу по ID

Обновлять задачу

Функциональность:

Используй Pydantic для валидации данных

Храни данные в памяти (например, в списке), без БД

🟡 2. API с подключением SQLite и SQLAlchemy

Уровень: начинающий
Цель: познакомиться с базами данных и ORM.

Описание задачи:
Расширь предыдущее ToDo API:

Используй SQLite в качестве БД

Подключи SQLAlchemy или SQLModel

Все операции (CRUD) работают с базой

Функциональность:

Асинхронные маршруты

Миграции с Alembic (опционально)

Валидация данных через Pydantic

🟠 3. API с регистрацией, JWT и авторизацией пользователей

Уровень: уверенный новичок / junior
Цель: реализовать систему пользователей с безопасностью.

Описание задачи:
Создай API с:

Регистрацией пользователей

Авторизацией по логину и паролю

Получением JWT токена

Защищёнными маршрутами, доступными только авторизованным пользователям

Функциональность:

Пароли хэшируются (bcrypt)

JWT токены через python-jose

Middleware или Depends для защиты маршрутов

🟠 4. Загрузка и хранение изображений

Уровень: junior
Цель: работа с файлами, формами, MIME-типами.

Описание задачи:
Реализуй API для:

Загрузки изображения (jpeg/png)

Сохранения его на сервере

Хранения информации в базе (название файла, путь, время загрузки)

Получения и отображения загруженных изображений через API

Функциональность:

Проверка MIME-типа

Ограничение размера файла

Асинхронная работа с файловой системой

🟡 5. Фоновая обработка и планировщик задач

Уровень: strong junior
Цель: познакомиться с фоновыми задачами и отложенным выполнением.

Описание задачи:
Сделай API, позволяющее пользователю загружать текст, который будет обрабатываться (например, подсчёт слов, генерация отчёта) в фоне.

Функциональность:

Использование FastAPI background tasks или Celery

Очередь задач (например, Redis)

Endpoint для проверки статуса задачи

🔵 6. Интеграция с внешним API + кэширование

Уровень: junior+
Цель: работа с внешними API и реализация кэша.

Описание задачи:
Создай API, которое:

Получает данные о погоде по названию города из внешнего API (например, OpenWeatherMap)

Кэширует данные на 10 минут, чтобы не делать частые запросы к внешнему API

Использует Depends и async подход

Функциональность:

Кэширование в памяти (functools.lru_cache, cachetools или Redis)

Обработка ошибок от стороннего API

🔵 7. Мини-сервис блогов с авторизацией, комментированием и ролями

Уровень: confident junior / начинающий middle
Цель: комплексная реализация API с пользователями и ролевой системой.

Описание задачи:
Реализуй API, которое:

Позволяет пользователям создавать статьи (посты)

Комментировать посты

Имеет роли: admin, author, user с разными правами доступа

Функциональность:

JWT авторизация

Ролевой доступ (например, через Depends)

БД с отношениями между моделями (OneToMany: пользователь → статьи, статьи → комментарии)

Возможность soft-delete

🔴 8. Асинхронный чат с WebSocket + хранение сообщений

Уровень: Middle
Цель: продвинутый функционал: WebSocket, работа в реальном времени.

Описание задачи:
Реализуй базовый чат:

Пользователь подключается по WebSocket

Может отправлять и получать сообщения в реальном времени

Все сообщения сохраняются в БД (с привязкой к пользователям)

Функциональность:

Асинхронный WebSocket обработчик

Хранение истории сообщений в базе

JWT авторизация при подключении к WebSocket

Broadcast сообщений всем подключённым пользователям (можно использовать starlette.websockets)